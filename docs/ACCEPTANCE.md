# 待用户验收

分支：review-experience-20260916。起点：prod d8641c5。正式分支不变。

## 启动

在本工作树执行 `node serve.cjs`，访问 http://localhost:4186/?view=home 。监听地址仅127.0.0.1。
持续显示待验收横幅；固定独立存储键；初次打开使用示例数据。不同浏览器有不同验收数据。

## 自动化结果

`node verify-experience.cjs`：
- 11页面 × 7宽度 = 77组：360/375/390/414/430/768/1440；无横向溢出，按钮点击区至少44px。
- 5个指定手机宽度 × 600px短视口：长标题/长地点编辑器无横向溢出，滚动后保存按钮可见。
- 编辑不新增重复记录；刷新保留；原ID与未知历史字段保留。
- 未保存Escape确认；保存失败保留输入并可重试。
- 心愿新增/编辑/完成/删除；删除关联记录后恢复待完成，完成日期有记录。
- 3图、2图、1图、无图保存，预览、移除；坏图报错后仍能保存文字。
- 空状态、旧结构记录；损坏数据阻止覆盖；Prod键哨兵值保持不变。
- 页面脚本错误0；独立浏览器上下文，不使用个人浏览器数据。

`node ../../skills/finesse-ui/scripts/detect.mjs --json index.html life.css experience.css`：P0=0。P2为HTML没有内嵌CSS戳、基线life.css的白色变量；正式样式校准层使用染色surface，保留原文件避免重做封面。

已查看首页桌面/手机、编辑器、详情、时间轴、清单、数字、足迹、美食、电影空状态、我的截图。截图在verification/experience。

## 人工验收重点与边界

请体验新增/编辑、未保存返回、完成心愿后查看记录、相册预览和年份筛选。
浏览器视口覆盖不等于真实手机软键盘测试；iOS原生日期弹层、系统字体和真实刘海safe-area仍需真机验收。
无真实地图坐标/地图SDK、账号/云同步、后台加载骨架、离线首开缓存、年度报告/分享卡片。当前本地产品无需假装远程加载；保存与图片读取已有明确状态。
本轮未将任何修改合并到prod或发布到正式站。
