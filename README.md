# Life Plus One 视觉交互 Demo

## 当前待验收版本

本分支为 `review-experience-20260916`。运行 `node serve.cjs`，打开 http://localhost:4186/?view=home 。页面持续显示“待验收”，使用独立验收存储，未连接或修改Prod数据。

本轮说明：[产品审计](docs/EXPERIENCE_AUDIT.md)、[Design System与扩展说明](docs/DESIGN_SYSTEM.md)。验证：`node verify-experience.cjs`。以下为基线版本记录，端口、存储键与编辑器说明以本节和新文档为准。

根据《Life_Plus_One_产品方案_V1.1_UI视觉规范版》第 11 章校准。保留天蓝 / 湖蓝、湖景、生活照片和现有信息架构。当前为浏览器交互原型，可在电脑和手机尺寸预览。

## 打开

直接用浏览器打开 `index.html`；或在此目录执行 `node serve.cjs`，访问 http://127.0.0.1:4173 。服务仅监听本机。

直达首页：http://127.0.0.1:4173/?view=home 。不带参数则展示进入封面。这是本机测试地址，不是公网部署。

当前入口加载 `life.css` 和 `life.js`。`styles.css`、`app.js` 为上一版保留文件，不再加载。

## 本版

- 蓝天湖景封面、轻浮动照片、微光、进入按钮反馈、页面和底部弹层转场；遵循系统减弱动态效果设置。
- 六大模块入口、愿望、城市列表、第一次、美食、电影、数字、时间轴、搜索和记录详情。
- 动态表单、示例图片选择、单张本机图片（2MB 以内）、收藏、二次确认删除、JSON 导出。
- 完成愿望后才写入已完成状态；同一条记录进入时间轴、第一次和城市统计；删除关联记录后愿望恢复为想做。
- 初始展示示例数据，人生天数在自行填写生日后计算；无虚构累积数字。

数据仅存当前浏览器。沿用旧版数据时读取旧键并保留原值，新内容存入 `life-plus-one-v2`。不同浏览器、不同端口和直接打开文件的数据互不相通。

图片由上一版生图模型生成，位于 `assets/`；原 Word 图位于 `reference-images/`，供设计核对。本次沿用原素材，使用本机 `finesse-ui` 技能，产品方案中的色彩、尺寸和既有架构优先。

## V1.1 视觉校准

- 统一品牌色、文本色、圆角、触控尺寸与 460px 内容宽度；低饱和分类卡与照片保留原有色彩。
- 首页 Hero、六宫格和最近记录层级统一；最近记录照片保持 72×64，日期与城市同层展示。
- +记录弹层采用 18px 类型卡圆角，标题 23px，手写文字仅保留底部情绪句。
- 表单新增字数计数、照片预览与移除、就近错误、保存中禁用；存储失败保留输入，允许重试。
- 第一次和时间轴按年/月分组；人生数字主卡可下钻；分类筛选和按钮触控区至少 44px。
- 减弱动态设置关闭位移动画。原文件备份位于 `verification/before-v11/`。

`verify.cjs` 验证原有功能；`verify-ui.cjs` 验证新规范和失败恢复。测试使用独立临时浏览器上下文，不写入用户浏览器数据。尚未接入后端，因此本次验证不代表云端弱网、账号同步或真机验收。

## 范围

尚未接入微信登录、云端数据库、真实地图和年度报告。图片为本机存储演示，正式版需改为压缩上传和对象存储。这一版用于确认视觉与关键记录流程。

## 验证

2026-09-15，使用独立浏览器上下文执行 `verify.cjs`，未写入用户的浏览器资料。通过：1440px 电脑与 320/390/460px 手机尺寸无横向溢出、六个模块入口、类型选择、动态字段、愿望取消及完成与删除联动、刷新后保留、第一次汇总、搜索、输入 HTML 转义、减弱动态效果，浏览器脚本错误为零。实际渲染截图在 `verification/`，封面、首页和记录选择页已人工查看。

验证脚本引用本机已安装的 Node Playwright 依赖和 Edge 路径，跨电脑运行时需调整。直接预览不依赖这些测试工具。
